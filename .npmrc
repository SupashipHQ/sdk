# Library peers are declared for consumers; do not pull full trees (e.g. react-native
# → @react-native/debugger-frontend) into the SDK workspace. Packages list devDependencies
# explicitly where tests/builds need them.
auto-install-peers=false
